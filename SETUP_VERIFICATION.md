# Проверка работоспособности улучшений

## ✅ Что было проверено и исправлено

### 1. Конфигурационные файлы
- ✅ `pyproject.toml` - синтаксис корректен
- ✅ `.pre-commit-config.yaml` - синтаксис корректен
- ✅ `requirements.txt` - обновлены версии пакетов

### 2. Исправления

#### Версии пакетов
- Изменено `ruff==0.1.0` → `ruff>=0.1.0` (более гибкая версия)
- Изменено `pre-commit==3.5.0` → `pre-commit>=3.5.0` (более гибкая версия)

#### Pre-commit конфигурация
- Обновлена версия `ruff-pre-commit` до `v0.1.15` (более стабильная версия)
- Удален `detect-secrets` (требует дополнительной настройки baseline файла)
- Добавлены дополнительные проверки: `check-merge-conflict`, `check-toml`

### 3. Скрипты
- ✅ Все скрипты созданы и имеют корректный синтаксис
- ✅ `scripts/format.py` - форматирование кода
- ✅ `scripts/lint.py` - проверка кода
- ✅ `scripts/check.py` - запуск всех проверок
- ✅ `scripts/test_setup.py` - проверка работоспособности

## 📋 Инструкция по проверке

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Проверка установки

Запустите скрипт проверки:

```bash
python scripts/test_setup.py
```

Должны быть установлены:
- ✅ ruff
- ✅ pre-commit

### Шаг 3: Настройка pre-commit

```bash
pre-commit install
```

### Шаг 4: Тестирование форматирования

```bash
# Проверка форматирования (без изменений)
ruff format --check app/

# Форматирование кода
ruff format app/
```

### Шаг 5: Тестирование линтинга

```bash
# Проверка кода на ошибки
ruff check app/

# Автоматическое исправление
ruff check --fix app/
```

### Шаг 6: Тестирование pre-commit

```bash
# Запуск хуков вручную для всех файлов
pre-commit run --all-files
```

## ⚠️ Возможные проблемы и решения

### Проблема: ruff не найден
**Решение:**
```bash
pip install ruff
```

### Проблема: pre-commit не найден
**Решение:**
```bash
pip install pre-commit
pre-commit install
```

### Проблема: Ошибки при запуске pre-commit
**Решение:**
```bash
# Обновить хуки до последних версий
pre-commit autoupdate
```

### Проблема: Ruff не может найти конфигурацию
**Решение:**
Убедитесь, что файл `pyproject.toml` находится в корне проекта.

## ✅ Итоговая проверка

После выполнения всех шагов проверьте:

1. ✅ `ruff --version` - показывает версию
2. ✅ `pre-commit --version` - показывает версию
3. ✅ `python scripts/test_setup.py` - все проверки пройдены
4. ✅ `ruff format --check app/` - код отформатирован
5. ✅ `ruff check app/` - нет критических ошибок
6. ✅ `pre-commit run --all-files` - все хуки работают

## 📝 Примечания

- Версии пакетов указаны с `>=` для большей гибкости
- `detect-secrets` был удален из pre-commit, так как требует дополнительной настройки
- Все скрипты используют относительные пути и должны работать из корня проекта

