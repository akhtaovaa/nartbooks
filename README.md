# NartBooks API

API для управления книгами месяца с системой авторизации через JWT токены.

## 🚀 Быстрый старт

### Требования
- Python 3.9+
- pip

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/nartbooks.git
cd nartbooks
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте .env файл с вашими настройками
```

5. **Инициализируйте базу данных:**
```bash
# База данных создастся автоматически при первом запуске
```

6. **Запустите сервер:**
```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу: **http://localhost:8000**

Документация API: **http://localhost:8000/docs**

---

## 📁 Структура проекта

```
nartbooks/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа приложения
│   ├── config.py            # Конфигурация
│   ├── database.py          # Настройка БД
│   ├── models.py            # SQLAlchemy модели
│   ├── schemas.py           # Pydantic схемы
│   ├── security.py          # Функции безопасности
│   ├── dependencies.py      # Зависимости FastAPI
│   ├── enums.py             # Перечисления
│   └── routers/             # API роутеры
│       ├── auth.py          # Авторизация
│       ├── books.py         # Книги
│       ├── users.py         # Пользователи
│       └── general.py       # Общие эндпоинты
├── requirements.txt         # Зависимости Python
├── pyproject.toml           # Конфигурация Ruff
├── .pre-commit-config.yaml  # Конфигурация pre-commit
├── .env.example             # Пример переменных окружения
├── AUTH_README.md           # Документация по авторизации
└── README.md                # Этот файл
```

---

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта (используйте `.env.example` как шаблон):

```env
# База данных
DATABASE_URL=sqlite:///./nartbooks.db

# JWT настройки
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Админ токен
ADMIN_TOKEN=my_secret_token

# MSG OVRX API (для отправки кодов)
MSG_OVRX_BASE_URL=https://msg.ovrx.ru
MSG_OVRX_API_KEY=your_api_key_here
```

⚠️ **Важно:** Никогда не коммитьте файл `.env` в репозиторий!

---

## 📚 API Документация

После запуска сервера доступна интерактивная документация:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

### Основные эндпоинты

#### Авторизация
- `POST /auth/send-code` - отправка кода верификации
- `POST /auth/verify-code` - верификация кода и получение токена

#### Пользователи
- `GET /me` - получение информации о текущем пользователе (требует авторизации)
- `PATCH /me` - обновление профиля пользователя
- `GET /users` - получение списка пользователей (только для админов)
- `GET /users/{id}` - получение конкретного пользователя

#### Книги
- `GET /books` - получение списка книг месяца (с пагинацией и поиском)
- `GET /books/{id}` - получение конкретной книги месяца
- `POST /books` - добавление книги месяца (только для админов)
- `PUT /books/{id}` - обновление книги месяца (только для админов)
- `DELETE /books/{id}` - удаление книги месяца (только для админов)

Подробная документация по авторизации: [AUTH_README.md](AUTH_README.md)

---

## 🧪 Разработка

### Настройка pre-commit хуков

После клонирования репозитория установите pre-commit хуки:

```bash
pre-commit install
```

Теперь перед каждым коммитом будут автоматически выполняться проверки кода.

### Форматирование кода

```bash
# Форматировать весь код
ruff format app/

# Проверить форматирование без изменений
ruff format --check app/
```

### Проверка кода (линтинг)

```bash
# Проверить код на ошибки
ruff check app/

# Автоматически исправить исправимые ошибки
ruff check --fix app/
```

### Запуск всех проверок

```bash
# Форматирование + линтинг
ruff format app/ && ruff check app/
```

---

## 🔐 Безопасность

### Production настройки

1. **Используйте PostgreSQL вместо SQLite:**
```env
DATABASE_URL=postgresql://user:password@localhost/nartbooks
```

2. **Установите сильный секретный ключ:**
```bash
# Генерация случайного ключа
openssl rand -hex 32
```

3. **Используйте переменные окружения для всех секретов**

4. **Настройте HTTPS** (обязательно для JWT токенов)

5. **Регулярно обновляйте зависимости:**
```bash
pip list --outdated
pip install --upgrade <package>
```

---

## 🚢 Деплой

### Рекомендации для продакшена

1. Используйте переменные окружения для всех конфигураций
2. Настройте логирование (например, через `logging` модуль)
3. Используйте reverse proxy (nginx) перед приложением
4. Настройте мониторинг и алерты
5. Регулярно делайте бэкапы базы данных

### Пример запуска в продакшене

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

---

## 📝 Лицензия

[Укажите лицензию]

## 👥 Авторы

- [Ваше имя]

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

