# Руководство по улучшению проекта: Кодовая база

## Вариант 1: Настройка инструментов качества кода и документации

Этот вариант улучшений направлен на повышение качества кода, его консистентности и удобства разработки.

---

## 1. Линтеры и форматтеры кода

### Проблема
Без автоматических проверок кода:
- Разные разработчики пишут код в разном стиле
- Ошибки стиля и потенциальные баги обнаруживаются поздно
- Сложно поддерживать единообразие в большом проекте

### Решение: Настройка Ruff (современный инструмент для Python)

**Ruff** - это быстрый линтер и форматтер для Python, который заменяет множество инструментов:
- Линтер (как flake8, pylint)
- Форматтер (как black)
- Сортировщик импортов (как isort)

#### Шаги установки:

1. **Добавить в requirements.txt:**
```txt
ruff==0.1.0
```

2. **Создать файл `pyproject.toml` в корне проекта:**
```toml
[tool.ruff]
# Игнорировать определенные правила
ignore = ["E501"]  # Длина строки

[tool.ruff.lint]
# Включить правила проверки
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]

[tool.ruff.format]
# Настройки форматирования
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint.isort]
# Настройки сортировки импортов
known-first-party = ["app"]
```

3. **Добавить скрипты в проект:**

Создать файл `scripts/format.py`:
```python
"""Автоматическое форматирование кода."""
import subprocess
import sys

def main():
    """Форматирует весь код проекта."""
    result = subprocess.run(
        ["ruff", "format", "app/", "scripts/"],
        capture_output=True,
        text=True
    )
    if result.returncode != 0:
        print(result.stderr)
        sys.exit(1)
    print("✅ Код отформатирован")

if __name__ == "__main__":
    main()
```

Создать файл `scripts/lint.py`:
```python
"""Проверка кода на ошибки."""
import subprocess
import sys

def main():
    """Проверяет код на ошибки."""
    result = subprocess.run(
        ["ruff", "check", "app/", "scripts/"],
        capture_output=True,
        text=True
    )
    if result.stdout:
        print(result.stdout)
    if result.returncode != 0:
        sys.exit(1)
    print("✅ Линтинг пройден успешно")

if __name__ == "__main__":
    main()
```

#### Использование:

```bash
# Форматировать весь код
ruff format app/

# Проверить код на ошибки
ruff check app/

# Автоматически исправить исправимые ошибки
ruff check --fix app/
```

#### Что это даст:

✅ **Единый стиль кода** - все файлы будут форматироваться одинаково  
✅ **Обнаружение ошибок** - потенциальные баги будут найдены до запуска  
✅ **Улучшение читаемости** - код будет легче читать и понимать  
✅ **Экономия времени** - меньше споров о стиле кода в code review  

---

## 2. Pre-commit хуки

### Проблема
- Код с ошибками попадает в репозиторий
- Нужно помнить запускать проверки вручную
- Разные разработчики могут забыть проверить код

### Решение: Настройка pre-commit

**Pre-commit** автоматически запускает проверки перед каждым коммитом.

#### Шаги установки:

1. **Добавить в requirements.txt:**
```txt
pre-commit==3.5.0
```

2. **Создать файл `.pre-commit-config.yaml` в корне проекта:**
```yaml
repos:
  # Ruff для форматирования и линтинга
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.0
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # Проверка секретов в коде
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: package.lock.json

  # Проверка YAML файлов
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
```

3. **Установить хуки:**
```bash
pre-commit install
```

#### Что это даст:

✅ **Автоматическая проверка** - код проверяется перед каждым коммитом  
✅ **Единые стандарты** - все разработчики используют одинаковые проверки  
✅ **Защита от ошибок** - код с ошибками не попадет в репозиторий  
✅ **Экономия времени** - не нужно помнить о ручных проверках  

---

## 3. Улучшение README

### Проблема
- Новым разработчикам сложно понять, как запустить проект
- Нет информации о структуре проекта
- Непонятно, какие переменные окружения нужны

### Решение: Создать подробный README.md

#### Структура README.md:

```markdown
# NartBooks API

API для управления книгами месяца с системой авторизации через JWT токены.

## 🚀 Быстрый старт

### Требования
- Python 3.9+
- pip

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/nartbooks.git
cd nartbooks
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте .env файл с вашими настройками
```

5. Инициализируйте базу данных:
```bash
# База данных создастся автоматически при первом запуске
```

6. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу: http://localhost:8000

Документация API: http://localhost:8000/docs

## 📁 Структура проекта

```
nartbooks/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа приложения
│   ├── config.py            # Конфигурация
│   ├── database.py          # Настройка БД
│   ├── models.py            # SQLAlchemy модели
│   ├── schemas.py           # Pydantic схемы
│   ├── security.py          # Функции безопасности
│   ├── dependencies.py      # Зависимости FastAPI
│   ├── enums.py             # Перечисления
│   └── routers/             # API роутеры
│       ├── auth.py          # Авторизация
│       ├── books.py         # Книги
│       ├── users.py         # Пользователи
│       └── general.py       # Общие эндпоинты
├── requirements.txt         # Зависимости Python
├── .env.example             # Пример переменных окружения
└── README.md                # Этот файл
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL=sqlite:///./nartbooks.db

# JWT настройки
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Админ токен
ADMIN_TOKEN=my_secret_token

# MSG OVRX API (для отправки кодов)
MSG_OVRX_BASE_URL=https://msg.ovrx.ru
MSG_OVRX_API_KEY=your_api_key_here
```

⚠️ **Важно:** Никогда не коммитьте файл `.env` в репозиторий!

## 📚 API Документация

После запуска сервера доступна интерактивная документация:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

Подробная документация по авторизации: [AUTH_README.md](AUTH_README.md)

## 🧪 Разработка

### Форматирование кода
```bash
ruff format app/
```

### Проверка кода
```bash
ruff check app/
```

### Автоматическое исправление
```bash
ruff check --fix app/
```

## 🚢 Деплой

### Production настройки

1. Используйте PostgreSQL вместо SQLite:
```env
DATABASE_URL=postgresql://user:password@localhost/nartbooks
```

2. Установите сильный секретный ключ:
```env
JWT_SECRET_KEY=$(openssl rand -hex 32)
```

3. Используйте переменные окружения для всех секретов

4. Настройте HTTPS (обязательно для JWT токенов)

## 📝 Лицензия

[Укажите лицензию]

## 👥 Авторы

- [Ваше имя]
```

#### Дополнительно: Создать .env.example

```env
# База данных
DATABASE_URL=sqlite:///./nartbooks.db

# JWT настройки
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Админ токен
ADMIN_TOKEN=my_secret_token

# MSG OVRX API
MSG_OVRX_BASE_URL=https://msg.ovrx.ru
MSG_OVRX_API_KEY=your_api_key_here
```

#### Что это даст:

✅ **Быстрый старт** - новые разработчики смогут запустить проект за минуты  
✅ **Документация** - вся важная информация в одном месте  
✅ **Меньше вопросов** - ответы на частые вопросы уже в README  
✅ **Профессионализм** - проект выглядит более зрелым и готовым к продакшену  

---

## 📊 Итоговые преимущества

После внедрения этих улучшений:

1. **Качество кода** ↑ - автоматические проверки найдут ошибки до продакшена
2. **Скорость разработки** ↑ - меньше времени на исправление стиля и багов
3. **Онбординг** ↑ - новые разработчики быстрее вольются в проект
4. **Консистентность** ↑ - весь код в едином стиле
5. **Профессионализм** ↑ - проект выглядит более зрелым

---

## 🎯 Следующие шаги

После внедрения этого варианта можно перейти к:
- **Вариант 2:** Тестирование (unit-тесты, интеграционные тесты)
- **Вариант 3:** CI/CD (автоматические проверки при push)
- **Вариант 4:** Мониторинг и логирование

